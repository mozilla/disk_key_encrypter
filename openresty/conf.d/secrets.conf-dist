# allowed_group : Users must be a member of this group to access the admin site
# This group must be the same as the group configured in OIDC_DESKTOP_CLAIM_GROUP for Django
set $allowed_group 'NAME OF GROUP GOES HERE';  # Leave this unset if all users are allowed access, for example on the public site

# OIDC client_id
set $client_id "abcdefgABCDEFG0123456abcdefgABCD";

# OIDC client_secret
set $client_secret "abcdefgABCDEFG0123456abcdefgABCDabcdefgABCDEFG0123456abcdefgABCD";
